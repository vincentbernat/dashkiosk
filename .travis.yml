language: node_js
node_js:
  - "0.10"
  - "0.12"
sudo: false
addons:
  apt:
    packages:
      - libavahi-compat-libdnssd-dev
before_install:
  - "npm install -g grunt-cli bower"
script:
  - "npm test"
  - "grunt dist"
